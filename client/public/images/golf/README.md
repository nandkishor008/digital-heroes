Photographs for the "golf" group.

Run `npm run fetch-images` from the project root to populate this folder from
the manifest at client/src/lib/images.js, or drop your own files here using the
filenames listed in that manifest.
